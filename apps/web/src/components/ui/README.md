# UI Components

This directory contains shadcn/ui components that can be installed as needed using:

```bash
npx shadcn@latest add [component-name]
```

Common components to install:
- button
- card
- input
- label
- select
- dialog
- dropdown-menu
- toast
- form
- table
- etc.

Each component will be added here as it's needed in the application.
